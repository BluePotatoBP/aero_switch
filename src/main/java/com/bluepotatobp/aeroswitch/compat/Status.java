package com.bluepotatobp.aeroswitch.compat;

/** Compatibility status of a mod row. {@link #UNTESTED} is synthesized for installed, uncatalogued mods. */
public enum Status {
    WORKS,
    PATCHED,
    BROKEN,
    UNTESTED
}
